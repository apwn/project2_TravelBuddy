body {
  font-size: 1.1em;
  background: url(<%= image_path("crossword.png") %>);
}

.maintitle {
  font-size: 3em;
  text-align: center;
  margin: 50px 0;
}

.mainimage {
  background: url(<%= image_path("worldmap.png") %>) no-repeat;
  background-size: cover;
  background-position: center;
  width: 85%;
  height: 500px;
  margin: auto;
  border-radius: 30%;
}

.brand-logo {
  margin-left: 30px;
}

.mainbenefits {
  margin: 50px 0;
  font-size: 1.2em;
}

.mainbenefits img {
  margin-top: 20px;
}
